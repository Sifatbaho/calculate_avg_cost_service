services:
  avg_service:
    build: .
    container_name: avg_service
    ports:
      - "50051:50051"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
